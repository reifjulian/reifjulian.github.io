source 'https://rubygems.org'

# Site is hosted by GitHub Pages. To upgrade, run `bundle update github-pages`.
gem 'github-pages', group: :jekyll_plugins

# Plugins
group :jekyll_plugins do
  gem 'jekyll-paginate'
  gem 'jekyll-gist'
  gem 'jekyll-watch'
  gem 'jekyll-sitemap'
  gem 'kramdown'
  gem 'rouge'
end
